{
    "installCommand": "pnpm install --no-frozen-lockfile",
    "functions": {
        "src/app/**/*.ts": {
            "maxDuration": 30
        },
        "src/app/**/*.tsx": {
            "maxDuration": 30
        }
    }
}